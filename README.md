# Sructured Output LLM

## Motivation
Enforcing structured output in an LLM

## How to setup

Using poetry:

```bash
poetry add structured_output_llm
```

Using pip:

```bash
pip install structured_output_llm
```

